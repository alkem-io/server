# Alkemio dev stack + AI engines.
#
# Base services come from quickstart-services.yml; AI-specific services
# (ChromaDB vector store + virtual-contributor engines) come from
# overlays/ai-stack.yml. Image version bumps land in those files, not here.

include:
  - quickstart-services.yml
  - overlays/ai-stack.yml
