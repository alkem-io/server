export enum FetchErrorCodes {
  NOT_FOUND = 'not_found',
  INTERNAL_ERROR = 'internal_error',
}

export enum SaveErrorCodes {
  NOT_FOUND = 'not_found',
  INTERNAL_ERROR = 'internal_error',
  FORBIDDEN = 'forbidden',
}
