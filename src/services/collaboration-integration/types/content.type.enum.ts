// The unified content-type enum lives in `@common/enums` so both the domain
// layer (lifecycle emitter) and this integration module share one definition.
export { CollaborationContentType } from '@common/enums/collaboration.content.type';
