export const defaultContributionInterval = 600;
export const defaultSaveInterval = 15;
export const defaultSaveTimeout = 10;
export const defaultCollaboratorModeTimeout = 60 * 30; // 30 minutes

export const resetCollaboratorModeDebounceWait = 1000;
// logically it must always be 1, since it's either multi-user or not
export const minCollaboratorsInRoom = 1;
