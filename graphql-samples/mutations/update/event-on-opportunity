mutation eventOnOpportunity($opportunityEventData: OpportunityLifecycleEventInput!) {
  eventOnOpportunity(opportunityEventData: $opportunityEventData) {
    id
    lifecycle {
      state
      nextEvents
    }
  }
}

variables:
{
  "opportunityEventData":
  {
    "ID": 2,
  	"eventName": "REFINE"
  }
}