mutation ConvertChallengeToSpace($convertData: ConvertChallengeToSpaceInput!) {
  convertChallengeToSpace(convertData: $convertData) {
    nameID,
    id
  }
}

query variables:
{
  "convertData":
  {
    "challengeID": "uuid"
  }
}
